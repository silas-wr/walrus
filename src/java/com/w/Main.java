/* The entry point for the W program in a Java project. Connects to the base system. */

package com.w;

import java.io.*

class Main {
  public static void main(String args[]) {
    if (args.length() != 0) {
      System.out.println("USAGE: java com.w.app.Main");
      return;
    }

    // After this, we connect this with everything else
  }
}
